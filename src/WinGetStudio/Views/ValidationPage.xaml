<Page
    x:Class="WinGetStudio.Views.ValidationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:model="using:WinGetStudio.Models"
    xmlns:localControls="using:WinGetStudio.Views.Controls"
    xmlns:localConverters="using:WinGetStudio.Converters"
    xmlns:models="using:WinGetStudio.Services.DesiredStateConfiguration.Explorer.Models"
    xmlns:vm="using:WinGetStudio.ViewModels"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    mc:Ignorable="d">

    <Page.Resources>
        <x:Double x:Key="SectionMinWidth">100</x:Double>
    </Page.Resources>

    <!-- Page layout -->
    <Grid x:Name="ContentArea" ColumnSpacing="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Page header -->
        <StackPanel Spacing="8">
            <!-- Description -->
            <TextBlock Margin="0,0,0,8" HorizontalAlignment="Left">
                <Run x:Uid="ValidationPage_Description"/>
                <Hyperlink NavigateUri="https://learn.microsoft.com/en-us/powershell/dsc/concepts/resources/operations?view=dsc-3.0">
                    <Run x:Uid="ValidationPage_LearnMoreLink"/>
                </Hyperlink>
            </TextBlock>

            <!-- Search input -->
            <localControls:ResourceAutoSuggestBox Text="{x:Bind ViewModel.SearchResourceText, Mode=TwoWay}" />
        </StackPanel>

        <Rectangle Height="1"
                   Fill="{ThemeResource SystemControlForegroundBaseLowBrush}"
                   HorizontalAlignment="Stretch"
                   Margin="0,8"
                   Grid.Row="1"/>

        <!-- Page content -->
        <Grid Grid.Row="2" ColumnSpacing="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" MinWidth="{StaticResource SectionMinWidth}"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*" MinWidth="{StaticResource SectionMinWidth}"/>
            </Grid.ColumnDefinitions>

            <!-- Properties -->
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!-- Settings header -->
                <TextBlock x:Uid="ValidationPage_SettingsText" FontWeight="Bold"/>

                <!-- Settings -->
                <localControls:MonacoEditor Grid.Row="1" Text="{x:Bind ViewModel.SettingsText, Mode=TwoWay}" Syntax="yaml" />

                <!-- DSC buttons -->
                <controls:UniformGrid Grid.Row="2" Rows="1" Columns="3" ColumnSpacing="8">
                    <controls:UniformGrid.Resources>
                        <Style TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                            <Setter Property="HorizontalAlignment" Value="Stretch"/>
                        </Style>
                    </controls:UniformGrid.Resources>
                    <Button x:Uid="ValidationPage_GetButton" Command="{x:Bind ViewModel.GetCommand}" HorizontalAlignment="Stretch" Grid.Column="0"/>
                    <Button x:Uid="ValidationPage_SetButton" Command="{x:Bind ViewModel.SetCommand}" HorizontalAlignment="Stretch" Grid.Column="1"/>
                    <Button x:Uid="ValidationPage_TestButton" Command="{x:Bind ViewModel.TestCommand}" HorizontalAlignment="Stretch" Grid.Column="2"/>
                </controls:UniformGrid>
            </Grid>

            <!-- Resizer -->
            <controls:GridSplitter Grid.Column="1" />

            <!-- Results output -->
            <Grid Grid.Column="2" RowSpacing="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Results header -->
                <TextBlock x:Uid="ValidationPage_ResultsText" FontWeight="Bold"/>

                <!-- Result output box -->
                <Grid Grid.Row="1">
                    <localControls:MonacoEditor Text="{x:Bind ViewModel.OutputText, Mode=OneWay}" Syntax="yaml" IsReadOnly="True" />

                    <!-- Copy button -->
                    <localControls:CopyButton Content="&#xE8C8;"
                                              Click="{x:Bind CopyResultsToClipboard}"
                                              VerticalAlignment="Top"
                                              HorizontalAlignment="Right"
                                              Background="Transparent"
                                              BorderThickness="0"
                                              Margin="10"/>
                </Grid>
            </Grid>
        </Grid>
    </Grid>
</Page>