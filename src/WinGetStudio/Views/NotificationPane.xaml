<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinGetStudio.Views.NotificationPane"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:WinGetStudio.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:i="using:Microsoft.Xaml.Interactivity" xmlns:notifications="using:WingetStudio.Services.VisualFeedback.Models"
    mc:Ignorable="d">
    <UserControl.Resources>
        <converters:DoubleToVisibilityConverter x:Key="CountToVisibilityConverter" GreaterThan="0" FalseValue="Collapsed" TrueValue="Visible" />
    </UserControl.Resources>

    <i:Interaction.Behaviors>
        <i:EventTriggerBehavior EventName="Loaded">
            <i:InvokeCommandAction Command="{x:Bind ViewModel.LoadedCommand}" />
        </i:EventTriggerBehavior>
        <i:EventTriggerBehavior EventName="Unloaded">
            <i:InvokeCommandAction Command="{x:Bind ViewModel.UnloadedCommand}" />
        </i:EventTriggerBehavior>
    </i:Interaction.Behaviors>

    <Grid x:Name="PaneRoot" Tag="{x:Bind}">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock Style="{ThemeResource SubtitleTextBlockStyle}">Notifications</TextBlock>
        <HyperlinkButton Grid.Row="1"
                         Style="{ThemeResource TextBlockButtonStyle}"
                         Command="{x:Bind ViewModel.ShowPreviousNotificationsCommand}">Show all notifications</HyperlinkButton>
        <ScrollViewer Grid.Row="2">
            <StackPanel Grid.Row="2" Spacing="12">
                <!-- Unread notifications section -->
                <ListView ItemsSource="{x:Bind ViewModel.UnreadNotifications}" SelectionMode="None">
                    <ListView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <StackPanel Spacing="4" />
                        </ItemsPanelTemplate>
                    </ListView.ItemsPanel>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0" />
                            <Setter Property="Margin" Value="0" />
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="notifications:NotificationMessage">
                            <InfoBar IsOpen="True"
                                     Severity="Informational"
                                     IsClosable="True"
                                     IsIconVisible="False"
                                     CloseButtonCommand="{Binding ElementName=PaneRoot, Path=Tag.ViewModel.NotificationReadCommand, Mode=OneWay}"
                                     CloseButtonCommandParameter="{x:Bind}"
                                     Title="{x:Bind Title}"
                                     Message="{x:Bind Message}">
                                <InfoBar.CloseButtonStyle>
                                    <Style TargetType="Button" BasedOn="{StaticResource InfoBarCloseButtonStyle}">
                                        <Setter Property="ContentTemplate">
                                            <Setter.Value>
                                                <DataTemplate>
                                                    <FontIcon Glyph="&#xE73E;" FontSize="{ThemeResource ContentControlFontSize}" />
                                                </DataTemplate>
                                            </Setter.Value>
                                        </Setter>
                                    </Style>
                                </InfoBar.CloseButtonStyle>
                            </InfoBar>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>

                <!-- Previous notifications section -->
                <Grid Visibility="{x:Bind ViewModel.ArePreviousNotificationsVisible, Mode=OneWay}">
                    <StackPanel Spacing="12"
                                Visibility="{x:Bind ViewModel.PreviousNotifications.Count, Mode=OneWay, Converter={StaticResource CountToVisibilityConverter}}">
                        <NavigationViewItemSeparator />
                        <TextBlock Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center">Previous notifications</TextBlock>
                        <ListView ItemsSource="{x:Bind ViewModel.PreviousNotifications}" SelectionMode="None">
                            <ListView.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Spacing="4" />
                                </ItemsPanelTemplate>
                            </ListView.ItemsPanel>
                            <ListView.ItemContainerStyle>
                                <Style TargetType="ListViewItem">
                                    <Setter Property="Padding" Value="0" />
                                    <Setter Property="Margin" Value="0" />
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                </Style>
                            </ListView.ItemContainerStyle>
                            <ListView.ItemTemplate>
                                <DataTemplate x:DataType="notifications:NotificationMessage">
                                    <InfoBar IsOpen="True"
                                             IsClosable="False"
                                             Severity="Informational"
                                             IsIconVisible="False"
                                             Title="{x:Bind Title}"
                                             Message="{x:Bind Message}" />
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackPanel>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
