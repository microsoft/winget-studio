<Page  
    x:Class="WinGetStudio.Views.ValidationPage"  
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"  
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"  
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:converters="using:CommunityToolkit.WinUI.Converters"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:ui="using:CommunityToolkit.WinUI"
    xmlns:model="using:WinGetStudio.Models"
    xmlns:selectors="using:WinGetStudio.Selectors"
    xmlns:usercontrols="using:WinGetStudio.Views.Controls"
    xmlns:localConverters="using:WinGetStudio.Converters"
    mc:Ignorable="d">

    <Page.Resources>
        <converters:BoolNegationConverter x:Key="BoolNegationConverter" />
        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityNegationConverter" TrueValue="Collapsed" FalseValue="Visible" />
        <converters:BoolToObjectConverter x:Key="BoolToIntConverter" TrueValue="1" FalseValue="0"/>
        <converters:BoolToObjectConverter x:Key="BoolToIntNegationConverter" TrueValue="0" FalseValue="1"/>
    </Page.Resources>

    <Grid x:Name="ContentArea" Padding="0" ColumnSpacing="32" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>


        <StackPanel Spacing="8" HorizontalAlignment="Left" Grid.Row="0">
            <TextBlock Margin="0,0,0,8">
                <Run x:Uid="ValidationPage_Description"/>
                <Hyperlink NavigateUri="https://learn.microsoft.com/en-us/powershell/dsc/concepts/resources/operations?view=dsc-3.0">
                    <Run x:Uid="ValidationPage_LearnMoreLink"/>
                </Hyperlink>
            </TextBlock>
            <TextBlock x:Uid="ValidationPage_ModuleInputHeader" Margin="0,0,0,8"/>
            <TextBox x:Uid="ValidationPage_ModuleInput" Width="Auto" Text="{Binding Title, Mode=TwoWay}"/>
        </StackPanel>

        <Rectangle Height="1"
                   Fill="{ThemeResource SystemControlForegroundBaseLowBrush}"
                   HorizontalAlignment="Stretch"
                   Margin="0,8"
                   Grid.Row="1"/>


        <Grid Grid.Row="2" ColumnSpacing="8" x:Name="MainGrid">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition />
            </Grid.ColumnDefinitions>

            <!-- Results output -->
            <Grid Width="Auto" ColumnSpacing="10" RowSpacing="12" Grid.Column="2" MinWidth="200">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock x:Uid="ValidationPage_ResultsText" FontWeight="Bold" Grid.Column="0" Grid.Row="0" VerticalAlignment="Center"/>
                <Button Command="{Binding ConvertYamlToUICommand}" Grid.Column="1" Grid.Row="0">
                    <TextBlock x:Uid="ValidationPage_ResultsToUIText"/>
                </Button>
                <Button Click="{x:Bind CopyResultsToClipboard}" Grid.Column="2" Grid.Row="0">
                    <TextBlock x:Uid="ValidationPage_CopyToClipboard"/>
                </Button>

                <TextBox Width="Auto"
                         Text="{Binding RawData, Mode=TwoWay}"
                         AcceptsReturn="True"
                         TextWrapping="Wrap"
                         VerticalAlignment="Stretch"
                         HorizontalAlignment="Stretch"
                         Grid.Row="1"
                         Grid.ColumnSpan="4"
                         IsReadOnly="True"/>
            </Grid>

            <!-- Resizer -->
            <controls:ContentSizer Grid.Row="2"
                                   Grid.RowSpan="2"
                                   Grid.Column="1"
                                   HorizontalAlignment="Stretch"
                                   VerticalAlignment="Stretch"
                                   TargetControl="{x:Bind PropertiesGrid}"/>

            <!-- Properties -->
            <Grid Grid.Column="0" x:Name="PropertiesGrid" MinWidth="300">
                <usercontrols:ConfigurationSettings Properties="{x:Bind ViewModel.Properties, Mode=OneWay}"
                                                Grid.Column="0"
                                                HorizontalAlignment="Stretch"/>

                <TextBlock x:Uid="ValidationPage_EmptyPropertiesMessage"
                       Visibility="{Binding IsPropertiesEmpty}"
                       Grid.Row="1"
                       Grid.Column="0"
                       HorizontalAlignment="Center"
                       Margin="0,50,0,0"
                       TextAlignment="Center"/>

                <Grid ColumnSpacing="8" Margin="0,12,0,0" VerticalAlignment="Bottom" Grid.Row="1" Grid.Column="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                        <!-- <ColumnDefinition Width="*"/> -->
                    </Grid.ColumnDefinitions>
                    <Button x:Uid="ValidationPage_GetButton" Command="{Binding GetCommand}" IsEnabled="{Binding ActionsEnabled}" HorizontalAlignment="Stretch" Grid.Column="0"/>
                    <Button x:Uid="ValidationPage_SetButton" Command="{Binding SetCommand}" IsEnabled="{Binding ActionsEnabled}" HorizontalAlignment="Stretch" Grid.Column="1"/>
                    <Button x:Uid="ValidationPage_TestButton" Command="{Binding TestCommand}" IsEnabled="{Binding ActionsEnabled}" HorizontalAlignment="Stretch" Grid.Column="2"/>
                    <!-- Export is implemented but due to a bug (probably) in dsc only works around 50% of the time-->
                    <!-- <Button x:Uid="ValidationPage_ExportButton" Command="{Binding ExportCommand}" IsEnabled="{Binding ActionsEnabled}" HorizontalAlignment="Stretch" Grid.Column="3"/> -->
                </Grid>
            </Grid>
        </Grid>
        
        

        <Rectangle Height="1"
                   Fill="{ThemeResource SystemControlForegroundBaseLowBrush}"
                   HorizontalAlignment="Stretch"
                   Margin="0,8"
                   Grid.Row="3"/>

        <TeachingTip
                     IsLightDismissEnabled="False"
                     Visibility="{Binding TestBannerVisible}"
                     IsOpen="{Binding TestBannerVisible, Mode=TwoWay}">
            <StackPanel>
                <TextBlock x:Uid="ValidationPage_TestResultHeader"/>
                <TextBlock Text="{Binding TestBannerText}"/>
            </StackPanel>
        </TeachingTip>


    </Grid>
</Page>